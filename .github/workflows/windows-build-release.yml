on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    runs-on: windows-latest      
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python 3.10
      uses: actions/setup-python@v3
      with:
        python-version: "3.10"
    - name: check dirs2
      shell: pwsh
      run: ls -Path 'D:\a\TranslateAndTTS\TranslateAndTTS\GUI\GUI_TranslateAndTTS'
